%nav.navbar.navbar-default{:role => "navigation"}
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle.collapsed{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to('Mtube', root_path, class: 'navbar-brand')
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li= link_to('電影', movies_path)
        %li= link_to('標籤', '')
        %li= link_to('演員', '')
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              = current_user.email
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li= link_to('Account', edit_registration_path(:user))
              %li= link_to('Logout', destroy_session_path(:user), method: :delete)
        - else
          %li= link_to('Join us', new_registration_path(:user))
          %li= link_to('Sign in', new_session_path(:user))